server:
  port: 1007

spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka/

log:
  appName: order-service

logging:
  level:
    ROOT: INFO
    org.spring.framework.cloud.config: DEBUG
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace
