package com.pm.spring.ema.gateway.service.config;

import org.springframework.context.annotation.Configuration;

/**
 * This class is now simplified. The API documentation grouping is handled automatically by the
 * spring.cloud.gateway.discovery.locator and the dedicated 'openapi' route in application.yml.
 */
@Configuration
public class SwaggerConfig {
  // No custom beans are needed anymore.
}
