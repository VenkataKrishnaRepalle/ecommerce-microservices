spring:
  application:
    name: category-service
  profiles:
    active: contact
  config:
    import: 'configserver:'
  cloud:
    config:
      name: category-service,config-client
      username: user
      password: password
      zipkin:
        baseUrl: http://localhost:9411
        sleuth:
          sampler:
            probability: 1
            spring:
              sleuth:
                probability=1:
                  0:

#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    lease-expiration-duration-in-seconds: 5
#    lease-renewal-interval-in-seconds: 2

log:
  appName: category-service

logging:
  level:
    org:
      springframework:
        cloud: DEBUG

