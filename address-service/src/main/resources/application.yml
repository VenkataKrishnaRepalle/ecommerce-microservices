spring:
  application:
    name: address-service
  profiles:
    active: address
  config:
    import: 'configserver:'
  cloud:
    config:
      name: address-service,config-client
      username: user
      password: password

log:
  appName: address-service

logging:
  level:
    ROOT: INFO
    org.spring.framework.cloud.config: DEBUG
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace