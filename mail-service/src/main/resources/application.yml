spring:
  application:
    name: mail-service
  profiles:
    active: mail
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      name: mail-service,config-client
      username: user
      password: password

log:
  appName: mail-service

logging:
  level:
    org:
      springframework:
        cloud: DEBUG